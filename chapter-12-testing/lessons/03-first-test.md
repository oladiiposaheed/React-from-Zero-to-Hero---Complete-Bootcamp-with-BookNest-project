# Task 3: First Test

Created: src/test/example.test.jsx

What it does:

- Renders a Hello component
- Checks that "Hello, World!" appears on screen

Key functions:

- render() — puts component on virtual screen
- screen.getByText() — finds text
- expect().toBeInTheDocument() — checks it exists

Result: 1 test passed ✅
